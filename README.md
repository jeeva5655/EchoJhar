
  # version_1_tour

  This is a code bundle for version_1_tour. The original project is available at https://www.figma.com/design/5eqxyir0wBftne364qsV1D/version_1_tour.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  